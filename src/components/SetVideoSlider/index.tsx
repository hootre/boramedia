import { FC, useEffect, useState } from 'react';
import { SliderWrapper } from './styles';

const SetVideoSlider: FC<{}> = ({ children }) => {
  return <SliderWrapper>{children}</SliderWrapper>;
};

export default SetVideoSlider;
